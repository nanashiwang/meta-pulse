# 来源与核验边界

原始读取日期：2026-09-07。以下为 v1.0 交付包中保留的项目来源记录。

v1.1 本次修改以当前会话的 `METAR-Frontend-v1.html`、`METAR-Frontend-v1.zip` 和用户列出的删除范围为源，已重新读取 personal-os 的 README、文件规则和外部工具规则；没有修改 GitHub、没有访问线上站点，也没有重新核验项目仓库状态。

## v1.0 原始设计的读取记录

仓库：`nanashiwang/meta-pulse`

- `README.md`：社区为主要入口、真实付费调用权益为增长内核；Answer 独立注册、new-api 可选绑定；正式社区域名及 `/blog/` 基址。读取内容对应 blob SHA `1a566451569fc0ef7a434907989696e49f67852e`。
- `docs/COMMUNITY.md`：社区 / 博客职责、双身份、绑定规则、网关边界、只读徽章、内容奖励、测试与红线。读取文件主要内容及后半段，blob SHA `c7945287dbbedc5417545878d2906950e46764c6`。
- `docs/ARCHITECTURE.md`：本次读取第 1–155 行，重点为产品体验、真实付费调用经济基础、new-api 关系与身份链路。blob SHA `19d6431c670e4dfec92768e767d120f20c77588b`。
- `AGENTS.md`：系统事实源、幂等、预算、绑定、内容和工程不变量。blob SHA `e29ac378619fac1c996643729b944bea7c889378`。
- 仓库目录树与 `sites/blog/docs/` 目录：核对 Monorepo、论坛插件、博客与部署结构。目录树响应中的提交标识为 `d0e3d1c3468dd6ed1535a009c4ad25e0760c9757`。

操作前也读取了 `nanashiwang/personal-os/ai-manual/README.md` 及外部工具、文件与项目管理规则。

## 没有冒充完成的事项

1. 公开站点 `https://metar.uk/` 在当前环境未能成功打开：网页工具未取得页面，容器请求也未解析到站点。因此没有实际查看线上截图、DOM、前端样式或生产运行结果。
2. 没有读到并验证精确的生产 API 控制台、Pulse 操作页、正式 API 文档和监控地址。原型对应配置留空，并提供配置能力。
3. 没有登录真实账号、发邮件、发帖、执行 SSO、调用模型、请求生产接口或发放额度。
4. 没有完整代码审计，也没有把仓库自述里程碑当作生产验收通过证明。
5. 原型里的作者、帖子、文章、人数、统计、周期号、等级、回馈金额、工单、监控条形图均为演示内容，不是实际业务数据。

## 设计推导与已实现情况的区别

发现、问答、知识库、权益、个人空间和运营配置，是这份新原型建议采用的前端组织方式。它们与仓库定位保持一致，但不代表现有后端已经逐项提供对应的 HTTP API 或布局扩展能力。

前端接口抽象、最终路由、支持工单与跨系统搜索等内容在交接说明中明确标记为待适配，不以拟议接口冒充真实端点。
