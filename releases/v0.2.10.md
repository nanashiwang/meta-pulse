# v0.2.10

为百度站长提供官方文件验证方式。网关在指定校验文件地址返回百度提供的原始内容，HTTP 与 HTTPS 都可读取，并将该文件标为 noindex。

- 入口只匹配固定文件名；普通站点页面保持 HTTPS 永久跳转，账户与管理认证规则不变。
- 校验响应不依赖静态构建目录，后续更新可持续保留所有权验证。
- 包含前序版本的 301 兼容、公开内容抓取、站点地图和部署目录保留修复。

验证：Nginx 集成检查覆盖两种协议的原始文件内容、noindex 和未授权文件名的正常跳转；发布使用完整 CI 与附件校验。
