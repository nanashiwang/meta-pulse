# v0.2.9

百度站长实际验证返回“未知原因:308”。本版本将 HTTP 到 HTTPS、www 到主域名的 GET/HEAD 永久跳转改为 301，使验证器可继续访问正式首页。

- 其他请求方法仍用 308，保留方法与请求体；HTTPS、证书校验和访问权限不变。
- 包含 Google、百度验证标签、公开内容爬虫查询例外、三份站点地图和构建期间的静态目录保留修复。

验证：真实 Nginx 检查 GET/HEAD 的 301、POST 的 308 以及跳转目标；部署脚本、社区收录检查与完整发布 CI。
